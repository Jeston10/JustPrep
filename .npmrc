# pnpm settings
# eslint-config-next references its plugins by name; pnpm 10 stopped hoisting them by default.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
# Fail on peer-dependency conflicts instead of silently resolving them.
strict-peer-dependencies=false
auto-install-peers=true
